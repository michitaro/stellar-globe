次に留意してください。

* こまめにgit commitする。
  * 少なくとも依頼の項目単位ではcommitすること。
  * 依頼に関係ないファイルはコミットしない
* 以下の作業をお願いすることになりますが、終わったらチェックリストにチェックを入れてください。
  * このように: `[ ]` → `[x]`

## 依頼

* [ ] non interactive test modeの導入

  frontend用のコンポーネントに


* [x] `.github/copilot-instructions.md`の整備

    `.github/copilot-instructions.md`に以下の内容を記してください。
    
    * 自然言語について
      * コード中のコメントは日本語、または英語で記述する。
      * markdownファイルは `.ja.md` と `.md` の両方を用意する。
        * `.ja.md`は日本語、`.md`は英語で書く。
        * 日本語版の内容が優先される。```
    * このプロジェクトの目的(`README.md`を参照。)
    * `.github/copilot-instructions.md`が実コードと乖離しないよう、コードの変更を常に反映させる。
    * 実装は細かい単位で動作確認を行う。
    * 必要に応じて`{stellar-globe,react-stellar-globe,react-draggable-dialog,app,python-integration}/docs`の内容を参照するということ
      * それが可能なように`docs`のインデックスを`.github/copilot-instructions.md`内に維持すること。
    * このプロジェクトはいくつかのコンポーネントに分かれており、それぞれの動作確認はそれぞれのディレクトリにcdする必要がある。
      動作確認の際には適切なディレクトリで行うこと。
  
  以降の依頼は`.github/copilot-instructions.md`の内容を前提としてください。

  * frontendのテストについて
  
    `npm run test`で実行するとinteractiveモードになるが、これはagent開発では都合が悪い。
    `npm run test:test`で非interactiveモード
  